{
  "name": "elnino-india",
  "version": "1.0.0",
  "description": "India El Niño Intelligence Dashboard — free, open-source climate monitoring",
  "type": "module",
  "scripts": {
    "dev": "observable preview",
    "build": "observable build",
    "deploy": "observable deploy",
    "clean": "rm -rf dist"
  },
  "dependencies": {
    "@observablehq/framework": "^1.9.0"
  },
  "engines": {
    "node": ">=18"
  }
}
